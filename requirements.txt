# Core (used by the algorithms + headless runner)
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
pyyaml>=6.0
openpyxl>=3.1

# UI + charts
streamlit>=1.32
plotly>=5.18

# Optional: run the .sql extracts against BigQuery and cache to parquet.
# Without these (or credentials) the app falls back to the bundled sample CSV.
google-cloud-bigquery>=3.0
db-dtypes>=1.1
pyarrow>=14.0
